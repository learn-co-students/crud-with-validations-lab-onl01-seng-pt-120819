<%= link_to "New Song", new_song_path %>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>
 
  <tbody>
    <% @songs.each do |song| %>
      <tr>
        <td><%= link_to song.title, song_path(song) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
 
<br>
 
